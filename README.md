# Fundo-IMS
Insurance Management System

This project was generated with Angular CLI version 18.0.7.

## Development server
Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`.

## Build
Run `ng build` to build the project.

## Further help
Use `ng help` or see the Angular CLI documentation.

## Icon assets & attribution
This project references the "Solar" icon set (Solar — Bold Duotone) for the main navigation and notification icons.

License: CC Attribution License (you must include attribution whenever the icons are used). Please add the original SVG files into the project under:

```
assets/icons/solar/
```

Recommended file names used by the layout (place matching SVGs):
- `home-duotone.svg`
- `dashboard-duotone.svg`
- `policies-duotone.svg`
- `analytics-duotone.svg`
- `vehicle-duotone.svg`
- `users-duotone.svg`
- `billing-duotone.svg`
- `notification-duotone.svg`
- `settings-duotone.svg`

Attribution (example — replace with the author/source you used):
```
Icons: Solar — Bold Duotone by Solar Icons. Licensed under CC BY (Attribution). https://solar-icons.example.com
```

If you want, I can add the actual SVGs and a proper LICENSE file for the icons (I can't fetch the icon files without access to the source). If you provide the set or a zip, I will drop them into `assets/icons/solar/` and wire them up automatically.